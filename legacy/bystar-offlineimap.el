;;; -*- Mode: Emacs-Lisp -*-


(message "ByStar offlineimap LOADING")

;; (when (bx:emacs24.5p)
;;   (require 'offlineimap)
;;   (add-hook 'gnus-before-startup-hook 'offlineimap)
;;   )

(provide 'bystar-offlineimap)
